{{!-- Handlebars para editar cada  uno de las Ordenes --}}
<div class="container p-4 col-auto ">
    <div class="row">
        <div class="col-md-4 mx-auto ">
            <div class="card bg-dark">
                <div class="card-body">
                    <h1 class="text-light text-center"> EDITAR PLAN DE TRABAJO </h1>
                        <form action="/links/edit/{{links.id}}" method="post">
                            <div class= "form-group">
                                <input type="text" value="{{links.title}}" class="form-control" name="title" placeholder="Titulo" autofocus>
                            </div>
                            <div class="form-group">
                                <textarea name="description" rows="2" class="form-control" placeholder="Descripción">{{links.description}}</textarea>
                            </div>
                            <div class="form-group">
                                <button class="btn btn-success btn-block bg-info"> GUARDAR CAMBIOS</button>
                            </div>
                        </form>
                </div>
            </div>
        </div>
    </div>
</div>